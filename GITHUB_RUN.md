# تشغيل التطبيق من داخل GitHub

## التشغيل التفاعلي داخل Codespaces

افتح المستودع `abdelatizarzori3-sys/smart-assistant-app` ثم اختر **Code → Codespaces → Create codespace on main**. سيقرأ Codespaces ملف `.devcontainer/devcontainer.json`، ويثبت الاعتمادات تلقائيًا، ويفتح منفذ التطبيق `3000`، ثم يبدأ خادم التطوير. افتح الرابط الذي يظهر في تبويب **Ports** لمعاينة التطبيق.

إذا احتاجت المصادقة أو خدمات الذكاء الاصطناعي إلى متغيرات بيئة، أضفها في **Codespaces Secrets** أو في بيئة التشغيل المحلية، ولا تضع قيمًا سرية داخل المستودع. يعمل التطبيق من دون أسرار خارجية فقط في الأجزاء التي لا تعتمد على المصادقة أو الخدمات المدارة.

## التشغيل المؤقت داخل GitHub Actions

من تبويب **Actions** اختر **تشغيل التطبيق مؤقتًا** ثم اضغط **Run workflow**. ينفذ الـ Workflow فحص الأنواع والاختبارات، ويبدأ خادم التطوير على المنفذ `3000`، ثم يجري فحص صحة محليًا ويحفظ سجل الخادم كـ Artifact. هذا التشغيل مؤقت داخل Runner وينتهي بانتهاء المهمة؛ لا ينتج رابطًا عامًا دائمًا ولا يستبدل الاستضافة الإنتاجية.

## التحقق المستمر

يستمر Workflow التحقق الموجود في `.github/workflows/ci.yml` بالعمل تلقائيًا عند الدفع وطلبات السحب، وينفذ `pnpm check` و`pnpm test` و`pnpm build`.

## الاستضافة الإنتاجية

للمعاينة العامة الدائمة استخدم النشر المُدار للتطبيق أو استضافة إنتاجية مناسبة. GitHub Actions مناسب للتحقق والبناء والتشغيل المؤقت، بينما Codespaces مناسب للتطوير التفاعلي داخل GitHub.
