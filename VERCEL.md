# النشر على Vercel

## الحالة الحالية

هذا المشروع ليس واجهة ثابتة فقط؛ فهو يجمع React وVite مع خادم Express وtRPC وDrizzle/MySQL ومصادقة OAuth وتخزين ملفات. لذلك لا يكفي وضع `vercel.json` بسيط أو نشر مجلد `dist` كي يعمل التطبيق كاملًا على Vercel. تشغيل الواجهة وحدها سيؤدي إلى فقدان مسارات الخادم والمصادقة وقاعدة البيانات.

النسخة الحية الحالية متاحة عبر الاستضافة المُدارة للمشروع:

[فتح الموقع الحي](https://smartassist-8btpufdw.manus.space)

## ما يظهر في المستودع

يحتوي `README.md` على رابط **Live Demo** ظاهر أعلى صفحة GitHub، بينما تبقى ملفات `README_AR.md` و`GITHUB_RUN.md` مصادر التوثيق والتشغيل داخل GitHub. هذا يحقق تجربة مشابهة لفكرة رابط الموقع الظاهر في مستودعات Vercel، دون الادعاء بأن خادم Express الحالي أصبح Serverless تلقائيًا.

## متطلبات نقل حقيقي إلى Vercel

للنشر الكامل على Vercel يجب أولًا تحويل نقطة تشغيل Express إلى Vercel Functions أو فصل واجهة React عن API، ثم ضبط مسارات OAuth وtRPC، وربط قاعدة MySQL خارجية، وإضافة متغيرات البيئة والأسرار من إعدادات المشروع. يجب بعد ذلك إنشاء اختبارات نشر منفصلة قبل تحويل النطاق العام.

حتى اكتمال هذا التحويل، يظل رابط Manus هو رابط الإنتاج الصحيح، ويُستخدم GitHub Actions للتحقق والبناء والتشغيل المؤقت داخل Runner، بينما يُستخدم Codespaces للتطوير التفاعلي داخل GitHub.
